import { SUPABASE_URL, SUPABASE_ANON_KEY, supabase } from './config.js?v=7';
const urlParams=new URLSearchParams(window.location.search);const qrCode=(urlParams.get('qr')||'').trim();let currentBusiness=null;let selectedRating=0;
async function init(){if(!qrCode){setText('biz-name','Invalid Link');setText('biz-subtitle','No QR code specified in URL.');return;}try{const response=await fetch(`${SUPABASE_URL}/functions/v1/public_qr_lookup`,{method:'POST',headers:{'Content-Type':'application/json','apikey':SUPABASE_ANON_KEY},body:JSON.stringify({qr_code:qrCode})});const data=await response.json();if(!response.ok)throw new Error(data?.error||`QR lookup failed (${response.status})`);const business=data?.business;if(!business){setText('biz-name','WonderQR');setText('biz-subtitle','This QR code is not activated yet. Please check back after activation.');return;}if(business.active!==true){setText('biz-name','WonderQR');setText('biz-subtitle','This QR code is not currently active.');return;}currentBusiness=business;setText('biz-name',currentBusiness.business_name||'');setupSocialLinks();setupStars();setupAdminButton();}catch(error){console.error('QR/business lookup failed:',error);setText('biz-name','Unable to load QR code');setText('biz-subtitle',`Please try again in a moment. (${error?.message||'Unknown error'})`);}}
function setText(id,text){const el=document.getElementById(id);if(el)el.innerText=text;}
function setupSocialLinks(){const socialBlock=document.getElementById('social-block');if(!socialBlock)return;const links=[['link-insta','instagram_url',v=>v],['link-yt','youtube_url',v=>v],['link-fb','facebook_url',v=>v],['link-wa','whatsapp_number',v=>`https://wa.me/${String(v).replace(/\D/g,'')}`],['link-call','phone_number',v=>`tel:${String(v).replace(/[^+\d]/g,'')}`],['link-justdial','justdial_url',v=>v],['link-pinterest','pinterest_url',v=>v],['link-x','x_url',v=>v]];let hasSocial=false;links.forEach(([id,field,toHref])=>{const e=document.getElementById(id);if(e)e.classList.add('hidden');const value=String(currentBusiness?.[field]||'').trim();if(value&&e){e.href=toHref(value);e.classList.remove('hidden');hasSocial=true;}});if(hasSocial)socialBlock.classList.remove('hidden');else socialBlock.classList.add('hidden');}
function setupAdminButton(){if(document.getElementById('owner-admin-access'))return;const button=document.createElement('button');button.id='owner-admin-access';button.type='button';button.textContent='Admin';button.style.cssText='margin-top:18px;padding:8px 18px;border:1px solid #d1d5db;border-radius:20px;background:#fff;color:#374151;font-size:.78rem;font-weight:600;cursor:pointer;';button.addEventListener('click',openOwnerAdmin);const card=document.querySelector('.card-container');const social=document.getElementById('social-block');if(social&&social.parentNode)social.parentNode.insertBefore(button,social);else if(card)card.appendChild(button);}
async function openOwnerAdmin(){const pin=window.prompt('Enter 4-digit Admin PIN');if(pin===null)return;if(!/^\d{4}$/.test(pin)){alert('Please enter exactly 4 digits.');return;}try{const response=await fetch(`${SUPABASE_URL}/functions/v1/verify_admin_pin`,{method:'POST',headers:{'Content-Type':'application/json','apikey':SUPABASE_ANON_KEY,'Authorization':`Bearer ${SUPABASE_ANON_KEY}`},body:JSON.stringify({business_id:currentBusiness.id,pin})});const result=await response.json();if(!response.ok||!result.ok||!result.owner_token){alert('Incorrect Admin PIN.');return;}window.location.href=`${window.location.origin}/wonderqr/admin.html?token=${encodeURIComponent(result.owner_token)}`;}catch(error){console.error('Admin PIN verification failed:',error);alert('Unable to verify PIN. Please try again.');}}
function setupStars(){const stars=document.querySelectorAll('#star-container .star');stars.forEach(star=>star.addEventListener('click',()=>{const rating=parseInt(star.getAttribute('data-rating'),10);if(!rating||!currentBusiness)return;selectedRating=rating;stars.forEach(s=>s.classList.toggle('selected',parseInt(s.getAttribute('data-rating'),10)<=rating));if(rating>=4){setText('biz-subtitle','Redirecting to Google Reviews...');redirectToGoogle();}else document.getElementById('feedback-form-container')?.classList.remove('hidden');}));}
function redirectToGoogle(){if(currentBusiness?.google_review_url)window.location.href=currentBusiness.google_review_url;else setText('biz-subtitle','Google Review link is not configured for this business.');}
const feedbackForm=document.getElementById('feedback-form');if(feedbackForm)feedbackForm.addEventListener('submit',async e=>{e.preventDefault();if(!currentBusiness||selectedRating<1||selectedRating>3){alert('Please select 1, 2, or 3 stars before submitting feedback.');return;}const messageEl=document.getElementById('fb-comments');const message=messageEl?messageEl.value.trim():'';if(!message){alert('Please enter your feedback.');return;}const submitButton=feedbackForm.querySelector('button[type="submit"]');if(submitButton){submitButton.disabled=true;submitButton.textContent='Sending...';}const{error}=await supabase.from('feedback').insert([{business_id:currentBusiness.id,rating:selectedRating,message}]);if(error){console.error('Feedback submission failed:',error);alert('Failed to send feedback: '+error.message);if(submitButton){submitButton.disabled=false;submitButton.textContent='Submit Feedback';}return;}document.getElementById('feedback-form-container')?.classList.add('hidden');document.getElementById('thank-you-msg')?.classList.remove('hidden');});init();