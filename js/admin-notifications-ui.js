import { enableAdminNotifications, disableAdminNotifications } from './notifications.js';
const token=new URLSearchParams(location.search).get('token')||localStorage.getItem('admin_auth_token');
const ready=()=>{if(document.getElementById('admin-notifications-card'))return;const anchor=document.getElementById('status-message');if(!anchor)return;const card=document.createElement('section');card.id='admin-notifications-card';card.className='card section';card.innerHTML='<h2>🔔 Notifications</h2><div style="display:flex;gap:8px;flex-wrap:wrap"><button id="enable-notifications-btn" class="btn primary" type="button">Enable Notifications</button><button id="disable-notifications-btn" class="btn secondary" type="button">Disable Notifications</button></div><div id="notification-status" class="hint">Enable notifications to receive messages from WonderQR.</div>';anchor.insertAdjacentElement('afterend',card);document.getElementById('enable-notifications-btn').onclick=async()=>{const b=document.getElementById('enable-notifications-btn');b.disabled=true;try{await enableAdminNotifications(token);document.getElementById('notification-status').textContent='Notifications enabled on this device.'}catch(e){document.getElementById('notification-status').textContent=e.message}finally{b.disabled=false}};document.getElementById('disable-notifications-btn').onclick=async()=>{const b=document.getElementById('disable-notifications-btn');b.disabled=true;try{await disableAdminNotifications(token);document.getElementById('notification-status').textContent='Notifications disabled on this device.'}catch(e){document.getElementById('notification-status').textContent=e.message}finally{b.disabled=false}}};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',ready);else ready();
